import { Meta, Story } from "@storybook/addon-docs";
import * as stories from "./text-field.stories.tsx";
import { TextField } from "../src";

<Meta title="TextField" component={TextField} />

# TextField

Here we can write documentation with MDX, importing stories from the `text-field.stories.tsx` file that showcase it's features.

<Story story={stories.basic} />
