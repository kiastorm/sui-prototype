import { Meta, Story } from "@storybook/addon-docs";
import * as stories from "./button.stories.tsx";
import { Button } from ".";

<Meta title="Forms/Button" component={Button} />

# Button

<Story story={stories.ButtonVariants} />

## Loading State

<Story story={stories.LoadingState} />

## Rendering Button-styled Links

`ButtonAsLink`s do not have `isActive`, `isDisabled` or loading props.

<Story story={stories.RenderAsLink} />
