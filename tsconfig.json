{
  "extends": "./tsconfig.base.json",
  "noErrorTruncation": true,
  "compilerOptions": {
    "paths": {
      "*": ["node_modules/*"],
      "@emotion/core": ["./index.d.ts"] // Else `css` prop of `@stitches/react` collides with `css` prop of Storybook's `@emotion/core`
    }
  }
}
