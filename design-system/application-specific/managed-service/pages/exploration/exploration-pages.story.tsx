import { AppLayout } from "../../one-offs/app-layout";
import "../../styles.css";
import { DetailSomeTablePage, mockRouter } from "./some-table-page";

export default {
  title: "Managed Service / Pages / Exploration",
};

export const DetailSomeTable = () => <DetailSomeTablePage />;
